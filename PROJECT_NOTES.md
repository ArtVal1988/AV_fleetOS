# AV_fleetOS — робочі нотатки для Claude

**Прочитай цей файл ПЕРШИМ, перш ніж вносити будь-які зміни.** Він живе в
репозиторії й оновлюється разом з кодом — тому це найнадійніше джерело
контексту між різними чатами/сесіями.

**ПРАВИЛО: оновлювати цей файл після КОЖНОГО кроку внесення правок**
(кожного коміту з реальною зміною коду чи логіки — не обов'язково для
дрібних CSS-правок на кілька пікселів). Секцію "Незавершене / потребує
уваги" тримати актуальною: прибирати виконане, додавати нове. Секцію
"Важливі уроки" поповнювати, якщо знайдено щось варте запам'ятовування
(новий клас багів, а не разова помилка). Коміт з оновленням нотаток можна
робити разом з кодовим комітом (не обов'язково окремим пушем).

## Що це за проєкт

Система управління автопарком для прокату авто (av-fcs.com, пов'язана з
TOPRENT). Український інтерфейс. Vanilla JS фронтенд в одному HTML-файлі
(без збірки/бандлера), Express + SQLite бекенд.

## Структура репозиторію

```
public/index.html   — увесь фронтенд (HTML+CSS+JS в одному файлі, ~8000+ рядків)
server/server.js    — Express застосунок, точка входу
server/db.js        — схема SQLite (better-sqlite3 / node:sqlite)
server/routes/       — auth.js, bookings.js, vehicles.js, stickers.js, settings.js, price-check.js
```

## Сервер (VPS) — структура папок

- `/var/www/AV_fleetOS` — git-клон репозиторію (сюди робиться `git pull`)
- `/var/www/AV_fleetOS-server` — **прод**, робоча копія, яку реально обслуговує pm2-процес `AV_fleetOS` (порт 3000, av-fcs.com)
- `/var/www/AV_fleetOS-staging` — **staging**, копія прод-версії (окрема БД!) на порту 3001, pm2-процес `AV_fleetOS-staging`
  - Доступ: `http://173.242.58.173:3001` (пряме IP:порт, без домену)
  - `.env` там ідентичний прод, окрім `PORT=3001`

## Робочий процес деплою (ВАЖЛИВО!)

1. Claude вносить правки локально в контейнері, комітить і пушить у GitHub (`git push origin main`)
2. Спершу деплоїмо **тільки на staging**:
   ```
   bash /root/update-staging.sh
   ```
3. Користувач тестує на `http://173.242.58.173:3001`
4. Тільки після явного підтвердження користувача — деплой на прод:
   ```
   bash /root/update.sh
   ```
5. **Завжди перевіряти після деплою** контрольними сумами:
   ```
   md5sum /var/www/AV_fleetOS-server/public/index.html /var/www/AV_fleetOS-server/server.js
   ```
   (для staging — аналогічно, шлях `/var/www/AV_fleetOS-staging/...`)

**У Claude немає SSH-доступу до сервера.** Усі команди деплою виконує
користувач вручну, Claude лише формулює команди й перевіряє результат.

**GitHub токен** протухає за кілька днів — якщо `git push` видає помилку
автентифікації, попросити користувача згенерувати новий Personal Access
Token (classic, scope `repo`) і оновити remote:
```
git remote set-url origin https://<TOKEN>@github.com/ArtVal1988/AV_fleetOS.git
```

## Важливі уроки цієї сесії (щоб не наступати на ті самі граблі)

1. **Ніколи не залишай непарні `<div>`.** Одного разу забутий закриваючий
   тег зламав усю верстку форми продовження оренди. Після будь-якої правки
   HTML-структури — перевіряти баланс тегів:
   ```python
   opens = html.count('<div'); closes = html.count('</div>')
   ```

2. **Кешування.** Express за замовчуванням додає ETag і не забороняє кеш
   статичних файлів — це спричиняло 304-відповіді й "залипання" старої
   версії сайту після деплою. Вже виправлено: `app.set('etag', false)` +
   `Cache-Control: no-store` на всі `/api/*` та на `public/index.html`.
   Якщо після деплою сайт показує старе — перше, що перевіряти.

3. **applyOrderLockState() залишає "мертві зони".** Ця функція виставляє
   inline `pointer-events:auto` на `#bm-block-1` (і подібні), щоб блок
   клієнта лишався клікабельним навіть при заблокованому замовленні. Це НЕ
   скидалось при закритті модалки — невидиме закрите вікно "ловило" кліки
   в тому місці екрана. Виправлено в `closeBookingModal()` — там тепер явно
   скидаються ці inline-стилі.

4. **mini-picker (z-index:400) вищий за modal-overlay (z-index:300).**
   Якщо `mini-picker` (спливаюча панель на порожній клітинці календаря)
   лишається відкритою і потім відкривається картка замовлення — mini-picker
   "просвічує" крізь неї. Виправлено викликом `closeMiniPicker()` на
   початку `editBooking()` і `openBookingModal()`.

5. **Нативний `<input type="date">`/`type="time"` в Chrome має дивний баг**
   всередині прокручуваних модалок: календар/час може "зникати-з'являтися"
   при переході фокуса між полями (відтворюється навіть в Інкогніто, навіть
   в Edge, навіть на секціях, які не редагувались). `blur()`/`preventDefault`
   на mousedown не завжди допомагають. **Рішення: власний JS-календар**
   (`openCustomDatePicker`/`openCustomTimePicker`, клас `.custom-dp-trigger`)
   замість нативного — вже реалізовано для полів Період/Отримання/
   Продовження/Повернення (дата+час). Значення полів лишаються в тому ж
   форматі (`YYYY-MM-DD`, `HH:MM`), усі існуючі `onchange`/`oninput`
   обробники продовжують працювати без змін.

6. **Після кожної правки HTML/JS — синтаксис-чек перед пушем:**
   ```python
   scripts = re.findall(r'<script>(.*?)</script>', html, re.S)
   # записати в тимчасовий .js і прогнати node --check
   ```
   Це кілька разів рятувало від деплою зламаного коду.

7. **Euro (€) прибрано скрізь, окрім поля "Депозит"** (свідоме рішення
   користувача) — валюта в "Вартість/Курс" продовження, "Загалом",
   "Оплачено" і основних "Фінансах" тепер лише $ і ₴.

8. **КРИТИЧНО: `/root/update.sh` на сервері довгий час НЕ копіював `server/`
   взагалі** — лише `public/`. Через це всі бекенд-зміни (server.js, нові
   маршрути типу price-check.js) "губились": пуш у GitHub проходив, скрипт
   відпрацьовував без помилок, а на прод реально нічого не потрапляло.
   Виявлено і виправлено 18.07.2026 через `verify-deploy.sh` (див. нижче).
   Актуальна версія `update.sh` копіює і `public/`, і
   `server.js`/`db.js`/`routes/*.js`, і робить `npm install`. Якщо колись
   знову хтось "спростить" update.sh — це перше, що перевіряти.

9. **Staging і прод мають ОКРЕМІ бази даних, що не синхронізуються.** Якщо
   код на проді підтверджено свіжий (verify-deploy.sh ✅), а функціонал,
   що залежить від ДАНИХ (не коду), виглядає "не працює" — перше питання:
   чи ці дані взагалі є в ПРОД-базі, чи їх додавали тільки на staging.
   Приклад: посилання на toprent.ua для конкретного авто треба вносити
   окремо і на staging, і на проді.

10. **Захардкоджений SEED DATA-блок спричиняв "блимання" видалених даних**
    (19.07.2026). У коді на верхньому рівні скрипта (виконується одразу при
    завантаженні, ще до `syncFromServer()`) були: 3 тестові стікери +
    73 тестові замовлення (`/* ── SEED DATA ── */`, виклики `addSticker(...)`
    і `addB(...)`). Кожне оновлення сторінки на секунду показувало ЦІ
    захардкоджені дані (включно з давно видаленими користувачем записами),
    доки реальні дані з сервера їх не перезаписували. Обидва блоки видалено
    повністю. **Урок:** якщо користувач скаржиться на "старі/видалені дані
    на секунду з'являються при оновленні" — шукати `.push(` виклики або
    `addXxx(...)` виклики НА ВЕРХНЬОМУ РІВНІ скрипта (не всередині функцій),
    які виконуються синхронно при парсингу, до будь-якого fetch до сервера.
    Перевірено: `CATEGORIES`/`ALL_VEHICLES` (флот авто) — чисті, без
    захардкоджених значень, весь список підвантажується з сервера.

11. **КРИТИЧНО #2: `update.sh`/`update-staging.sh` НЕ копіювали
    `server/package.json`** (виявлено 19.07.2026, спричинило прод 502).
    Коли додався новий npm-пакет (`multer` для `/api/documents`), скрипт
    деплою копіював `server.js`/`db.js`/`routes/*.js`, але НЕ
    `package.json` — тому `npm install --production` на сервері бачив
    СТАРИЙ package.json (без multer) і чесно рапортував "up to date",
    нічого не встановлюючи. Процес падав з `Cannot find module 'multer'`.
    Виправлено: обидва скрипти тепер явно копіюють і `package.json` теж.
    **Урок:** при додаванні НОВОЇ npm-залежності — завжди перевіряти, що
    деплой-скрипт копіює `package.json`, а не лише файли коду. Якщо після
    деплою 502 і в логах `Cannot find module` — перше, що перевіряти:
    `diff /var/www/AV_fleetOS/server/package.json
    /var/www/AV_fleetOS-server/package.json`.

## Інструменти для деплою/перевірки на сервері

- `/root/update.sh` — деплой на **прод** (git reset --hard + копіювання
  public/, server.js, db.js, package.json, routes/* + npm install +
  pm2 restart AV_fleetOS)
- `/root/update-staging.sh` — те саме, але на **staging** (порт 3001,
  окрема БД, pm2-процес AV_fleetOS-staging)
- `/root/verify-deploy.sh [prod|staging]` — звіряє md5 кожного файлу з
  git-репозиторію проти реально задеплоєної версії, друкує ✅/❌ по кожному
  файлу. **Використовувати після КОЖНОГО деплою** замість надсилання
  довгих md5-хешів вручну (зручніше на мобільному).

## Незавершене / потребує уваги

- **`/api/documents`** (routes/documents.js) — ✅ РЕАЛІЗОВАНО й підтверджено
  робочим (19.07.2026). Реальне серверне зберігання фото документів (не
  base64!) через `multer`, нова таблиця `documents`, файли лежать у
  `server/uploads/Документи/<номер> <назва авто>/` (людинозрозуміла
  структура для ручного перегляду по SSH). Галерея з навігацією ‹ › між
  кількома фото одного документа реалізована окремо (`viewAttachmentGallery`,
  не чіпає старий `viewAttachment` для інших місць типу файлів страхового
  випадку, які й досі на base64-підході — це свідомо, щоб не ламати робоче).
  Прикріплення/видалення файлів доступне лише в режимі "Редагувати"
  (керується через CSS-клас `.dm-edit-active`, НЕ inline-стиль — вхід у
  edit-mode не перерендерює HTML, тож inline-стилі, обчислені при
  попередньому рендері, там просто "застрягають").

- **`/api/price-check`** — ✅ ПОВНІСТЮ ПІДТВЕРДЖЕНО РОБОЧИМ на staging і на
  проді (18.07.2026). Парсить сторінку авто на toprent.ua (regex по тексту,
  без cheerio), повертає тарифи+заставу+характеристики.

- **Власний календар/час (custom-dp/custom-tp)** — підтверджено робочим
  ("Календар — гуд").

- **Мобільна адаптація (18.07.2026) — ✅ ЗАВЕРШЕНО, все підтверджено:**
  1. Характеристики авто на мобільній: перший тап поза попапом лише закриває
     його (не відкриває заодно форму статусів під ним) — глобальний
     capture-phase click-interceptor, лише для `window.innerWidth<=640`.
  2. Іконки ТО/Страхування/Документи в попапі характеристик — компактні,
     видимі на мобільній (додано звичайний `@media(max-width:640px)` на
     заміну ненадійного `@container` query).
  3. Панель "Транспортні засоби" — full-screen на мобільній, заголовок і
     пошук переносяться, кнопки дій під кожним авто йдуть на весь рядок
     (класи `.veh-row`, `.veh-row-actions`, `#veh-panel-header`).
  4. Модалка "Ремонти"/ТО — вже мала непогану базову мобільну адаптацію
     (картки замість таблиці); докручено km-meta-row (перенесення) і кнопки
     зверху модалки.
  5. Модалка "Страхування" — сітки `.im-fields`/`.im-fields-3` (були 2-3
     колонки) стек в 1 колонку на мобільній, платежі КАСКО переносяться.
  6. Модалка "Документи" — сітки техпаспорта/VIN (вже покрито спільним
     правилом), полів власника/довіреної особи (`.dm-person-fields`, було
     3 колонки) — тепер 1 колонка.

  Загальний патерн: усі виправлення — це точкові `@media(max-width:640px)`
  правила з `!important`, що стають в кінці існуючого мобільного блоку CSS
  (~рядок 165-290). Якщо колись знадобиться ще одна сторінка/модалка під
  мобільну — шукати аналогічні `grid-template-columns` без мобільного
  оверрайду, це найпоширеніша причина поламаної мобільної версії в цьому
  коді.

  **Доповнення (18.07.2026, альбомна орієнтація):** єдиний
  `@media (max-width:640px)` не спрацьовував при повороті телефону
  горизонтально (ширина зростає >640px). Виправлено:
  - Медіа-запит розширено: `@media (max-width:640px), (max-height:500px)
    and (orientation:landscape)` (той самий блок CSS, просто ширша умова).
  - Додано JS-хелпер `isMobileViewport()` (біля `dStr()`), що дублює ту саму
    логіку — і ЗАМІНЕНО ним усі розрізнені перевірки
    `window.innerWidth<=640` в JS (сайдбар, характеристики, тощо), щоб
    JS-поведінка не розходилась із CSS.
  - Окремий і важливий баг: відновлення стану бічного меню при завантаженні
    сторінки застосовувало "згорнутий десктопний" стан (`sb-collapsed`,
    56px, іконки) **незалежно від того, мобільний зараз режим чи ні** —
    через це на мобільному/в альбомній орієнтації меню виглядало
    "по-десктопному" замість повністю прихованого. Виправлено: тепер
    `sb-collapsed` відновлюється лише за умови `!isMobileViewport()`.
    **Урок:** будь-яка логіка відновлення UI-стану з localStorage має явно
    враховувати поточний viewport, а не просто читати збережений прапорець.

  **Доповнення (18.07.2026, реструктуризація вікна замовлення):**
  - Блоки "Період" + "Додаткові послуги" + "Фінанси" об'єднано в ОДИН
    візуальний блок (внутрішні під-блоки більше не `.msec`, а
    `.msec-inner-sep` — просто розділювач). "Продовження оренди" винесено
    в ОКРЕМИЙ блок `#bm-block-ext-section`, розташований ПІСЛЯ Фінансів.
  - Функція, що ховає блоки для спецстатусів (repair/investor/storage/
    personal), і досі керує `bm-block-extras`/`bm-block-3` окремо (їхні ID
    збережено — просто змінено CSS-клас), тому ця логіка не зламалась.
    Список `['bm-block-1','bm-block-extras','bm-block-3','bm-block-4',
    'bm-block-5']` доповнено `'bm-block-ext-section'`.
  - Поля дат "з"/"до" в Продовженні переведено з `type="date"` на
    `type="text" readonly` з класом `.ext-date-short` — показують компактний
    формат `ДД.ММ.РР`, а РЕАЛЬНЕ значення (ISO `YYYY-MM-DD`) зберігається в
    `data-iso`. Читати/писати ЛИШЕ через хелпери `getExtDateField(id)` /
    `setExtDateField(id, iso)` (біля `addOneDayStr`) — НІКОЛИ напряму через
    `.value` для цих двох полів. `customDpPick`/`openCustomDatePicker`
    автоматично розпізнають `.ext-date-short` і працюють з `data-iso`.

  **Доповнення (18.07.2026, виділення тексту в Документах):**
  Поля перегляду (VIN, номер техпаспорта тощо) мали `pointer-events:none`
  у режимі перегляду — це блокувало НЕ ЛИШЕ редагування, а й виділення/
  копіювання тексту. Замінено на `readOnly`/`disabled` (JS-керовані через
  `applyDocsFieldEditability()`, викликається в `toggleDocsEditMode()` і в
  кінці `renderDocsModalBody()`) — це дозволяє виділяти/копіювати текст
  завжди, блокуючи лише фактичне редагування. **Урок:** `pointer-events:
  none` — це "молоток", який блокує геть усе (клік, фокус, виділення).
  Якщо мета — заборонити РЕДАГУВАННЯ (не перегляд/копіювання), правильний
  інструмент — `readonly`/`disabled`, а не `pointer-events`.

## Стиль спілкування

Користувач спілкується українською, хоче конкретних команд для копіювання
в термінал, а не просто пояснень. Після кожної правки — конкретні md5sum
для звірки. Дуже цінує, коли Claude сам знаходить першопричину бага через
навідні консольні команди (`getBoundingClientRect`, `activeElement`,
`outerHTML` тощо), а не просто вгадує.
